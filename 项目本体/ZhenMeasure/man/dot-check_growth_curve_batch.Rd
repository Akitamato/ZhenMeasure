% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zhenm_qc_utils.R
\name{.check_growth_curve_batch}
\alias{.check_growth_curve_batch}
\title{批量生长曲线质控：逐头判定是否删除（issue #22）}
\usage{
.check_growth_curve_batch(daily_data, min_r2)
}
\arguments{
\item{daily_data}{日级表（需含 animal_id / record_date / daily_weight_g）}

\item{min_r2}{R² 阈值}
}
\value{
list(animals_to_delete, n_insufficient, n_low_r2)；
  animals_to_delete 顺序与 unique(daily_data$animal_id) 一致
}
\description{
对日级数据中每个个体做二次曲线拟合，点数不足（< 10 个有效体重点）或
R² 低于阈值即列入删除名单。原实现在 run_zhen_measure() 内以循环 + `c()`
追加实现（O(n²) 复制）且每头做一次全表子集；此处改为一次 split 取各头
有效体重点 + 预分配判定向量，判定集合与计数口径不变。
}
\keyword{internal}
