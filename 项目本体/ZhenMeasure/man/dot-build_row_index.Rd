% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zhenm_qc_utils.R
\name{.build_row_index}
\alias{.build_row_index}
\title{Build an animal_id -> row-index lookup (issue #36)}
\usage{
.build_row_index(dt, id_col = "animal_id")
}
\arguments{
\item{dt}{数据表}

\item{id_col}{个体列名}
}
\value{
具名 list，名字为个体 ID，值为该个体在 dt 中的升序行号向量
}
\description{
循环内反复 `which(dt$animal_id == id)` 是 O(头数 × 行数) 的全表扫描：
扬翔生产规模（668 头 × 179 万行）约 12 亿次比较，实测单这一句 8.1 s
（改用查表 0.35 s，23×）。改为循环外建表一次、循环内按 id 查表。
}
\details{
关键约束：返回的行号在每组内保持升序，且**不改变表序**，因此所有
`dt[idx, ...] :=` / `data.table::set(dt, idx, ...)` 形式的按行号回写
语义与 which() 完全一致（不采用 setkey 方案，它会重排表序）。
}
\keyword{internal}
